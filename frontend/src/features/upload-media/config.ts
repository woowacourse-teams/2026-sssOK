/**
 * #75 에서 정한 값들. 근거는 docs/frontend/UPLOAD_FLOW.md 의 "아직 정하지 않은 것" 절에 있다.
 * 실기기·실제 행사장에서 회선 속도를 재면 다시 볼 값이다.
 */

/**
 * 동시에 올리는 파일 개수.
 *
 * 발급 링크는 **전부 같은 순간에** 10분 시계를 걸어서, 순차로 올리면 뒤쪽 파일이
 * 자기 차례 전에 만료된다 (30장 기준 약 783초 > 600초). 동시 3개면 약 261초라 여유가 있다.
 * 반대로 너무 높이면 대역폭이 나뉘어 진행 바가 다 같이 멈춰 있는 모양이 된다.
 */
export const UPLOAD_CONCURRENCY = 3;

/**
 * 한 파일에 대해 프론트가 스스로 재발급받아 다시 올리는 횟수.
 * 서버 한도는 5회지만, 다 쓰면 실패가 확정될 때까지 사용자를 너무 오래 붙잡는다.
 */
export const MAX_AUTO_RETRY = 2;

/**
 * 재시도 전 대기(ms). 순간적인 5xx 에 같은 속도로 다시 부딪히는 걸 피한다.
 * 길이는 `MAX_AUTO_RETRY` 와 같아야 한다.
 */
export const RETRY_BACKOFF_MS = [500, 1500];
